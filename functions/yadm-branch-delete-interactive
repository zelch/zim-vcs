# vim:et sts=2 sw=2 ft=zsh
builtin emulate -L zsh

local -a remotes
if (( ${*[(I)(-r|--remotes)]} )); then
  remotes=(${^*:#-*})
else
  remotes=(${(f)"$(command yadm rev-parse --abbrev-ref ${^*:#-*}@{u} 2>/dev/null)"}) || remotes=()
fi
if command yadm branch --delete ${@} && \
    (( ${#remotes} )) && \
    read -q "?Also delete remote branch(es) ${remotes} [y/N]? "; then
  print
  local remote
  for remote (${remotes}) command yadm push ${remote%%/*} :${remote#*/}
fi
