# vim:et sts=2 sw=2 ft=zsh
builtin emulate -L zsh

local yadm_root
yadm_root=$(yadm-root) || return 1

# we are in a yadm repository. add parameters to .yadmignore
for file (${@}) print ${file} >>! ${yadm_root}/.yadmignore
